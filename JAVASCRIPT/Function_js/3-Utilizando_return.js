// Utilizando a palavra return

// Atribuindo uma função a uma variavel

function subtracao(a, b){
    let resultado = a - b;
    return resultado;
}
let resultado = subtracao(54, 32);
console.log(resultado);

// Retornando o resultado da função com o console.log

function subFunction(x, y){
return x + y;
}

console.log(subFunction(5, 34))
